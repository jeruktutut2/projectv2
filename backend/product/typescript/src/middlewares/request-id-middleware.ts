// import { NextFunction } from "express";
// import { string } from "zod";

// export const getRequestId = async (request: CustomRequest, response: Response, next: NextFunction) => {
//     request.requestId = request.headers.get("X-REQUEST-id") ?? ""
//     next()
// }